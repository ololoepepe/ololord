#ifndef WITHPOSTS_H
#define WITHPOSTS_H

#include "../global.h"

#include <string>

struct OLOLORD_EXPORT WithPosts
{
    std::string bannedForText;
    std::string bumpLimitReachedText;
    std::string closedText;
    std::string fixedText;
};

#endif // WITHPOSTS_H
